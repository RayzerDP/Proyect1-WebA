import { MenuIconDirective } from './menu-icon.directive';

describe('MenuIconDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuIconDirective();
    expect(directive).toBeTruthy();
  });
});
